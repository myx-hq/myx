# Benchmarks

This directory tracks non-gating benchmark snapshots for the MVP loop.

Warm-cache add benchmark:

```bash
./scripts/benchmark-warm-cache-add.sh --out ./benchmarks/warm-cache-add.YYYY-MM-DD.json
```

Notes:

- This benchmark is informational for trend tracking.
- It is not a release gate for MVP correctness.
